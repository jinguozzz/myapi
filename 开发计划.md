# MyAI Companion 开发计划书（v1.0）

> 依据《需求文档.txt》对比当前实现，列出未完成功能与后续开发排期。

## 1. 当前实现状态总览

| 模块 | 已完成 | 未完成 |
| --- | --- | --- |
| 模型管理 | M-01~M-05 全部完成（真实持久化 + 加密 Key + 请求头 + 参数） | — |
| 对话聊天 | C-02 发送 / C-03 真实流式 / C-04 轻量 Markdown / C-06 复制 / C-08 停止 / C-09 切换模型 | C-01 新建对话、C-05 代码高亮、C-07 重新生成、C-10 上下文长度 |
| 对话历史 | — | H-01~H-05 全部（当前仅为演示数据，未持久化） |
| 设置 | S-01 主题 / S-02 字体 / S-05 关于 | S-03 真实清除、S-04 导入导出配置 |
| 语音/附件 | 占位入口（模拟识别，未接真实 STT） | 真实语音识别、图片/文件上传 |

## 2. 未实现功能清单

### 2.1 对话聊天（P0 优先）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| C-01 | 新建对话 | 会话生命周期管理：新建、当前会话隔离、回到空会话 |
| C-07 | 重新生成 | 重新请求最后一条 AI 回复（重新流式输出） |
| C-05 | 代码高亮 | 用 `flutter_highlight` 或 highlight.js 包替换代码块占位渲染 |
| C-10 | 上下文长度 | 按最近 N 轮截断请求消息（防 Token 溢出） |

### 2.2 对话历史（P0，需本地持久化）✅ 已完成

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| H-01 | 历史列表 | ✅ SQLite 持久化，列表展示标题 + 时间戳 + 消息数 |
| H-02 | 删除对话 | ✅ 左滑/菜单删除（二次确认，级联删消息） |
| H-03 | 重命名对话 | ✅ 默认取首条用户消息，可手动改名 |
| H-04 | 搜索对话 | ✅ 按标题/内容关键词过滤 |
| H-05 | 导出对话 | ✅ 导出为 .md 并分享（share_plus） |

### 2.3 设置（P1）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| S-03 | 数据清除 | 真正清空会话库与模型配置 |
| S-04 | 导入/导出配置 | 模型配置导出/导入 JSON（file_picker） |

### 2.4 增强与扩展（P2 / V2.0）

- **真实语音输入**：接入系统语音识别（speech_to_text 包）替代占位模拟
- **附件功能**：图片识别 / 文件上传（对接支持多模态的模型）
- **完整 Markdown**：升级 flutter_markdown + 代码高亮，补齐表格等语法
- **消息长按菜单**：复制 / 重新生成 / 删除单条消息（现为一键复制最近回复）
- **主题扩展**：跟随系统已支持；补充自定义霓虹主题色

## 3. 开发排期

| 阶段 | 周期 | 内容 | 对应需求 |
| --- | --- | --- | --- |
| Phase A | 1 周 | 会话持久化（SQLite + Hive）：C-01 新建对话、H-01~H-03 | C-01, H-01~H-03 |
| Phase B | 3~4 天 | C-07 重新生成、C-05 代码高亮、C-10 上下文长度、H-04 搜索 | C-05/07/10, H-04 |
| Phase C | 3 天 | S-03 真实清除、S-04 导入导出、H-05 导出对话 | S-03/04, H-05 |
| Phase D | 视需要 | 真实语音输入、附件上传、Markdown 升级、自定义主题 | V2.0 方向 |
| Phase E | 收尾 | 性能指标验证（冷启动 ≤3s、流式首字 ≤500ms、包体积 ≤50MB）、错误重试机制、iOS 打包 | 非功能需求 |

## 4. 非功能需求核查项（未完成）

- [ ] 冷启动时间 ≤ 3s（需 Profile 测试）
- [ ] 列表滚动帧率 ≥ 60fps（长会话需做消息虚拟化/分页）
- [ ] 流式首字延迟 ≤ 500ms（已基本满足，需实测）
- [ ] Release 包体积 ≤ 50MB
- [ ] 网络请求失败重试机制（需求文档风险项）
- [ ] 大对话内存优化：限制消息数（如 200 条）自动截断

## 5. 风险与注意事项

- **API Key 安全**：已用 Secure Storage；注意不写入日志、不在导出配置时含明文 Key（导出时应脱敏或提示）
- **流式解析容错**：已跳过异常行；建议补充部分失败重连
- **iOS 审核**：确保明确「用户自备 Key」，无违规内容
- **SQLite 会话迁移**：初期结构简单，注意为后续升级预留版本迁移

## 6. 建议实施顺序（推荐从 Phase A 开始）

1. **先做会话持久化**（Phase A）—— 当前对话数据不落地，是体验最大短板
2. **再做对话增强**（Phase B）—— 提升使用效率
3. **设置完善 + 收尾验证**（Phase C/E）
